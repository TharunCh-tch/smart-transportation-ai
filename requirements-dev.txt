# Dev/test/lint tooling. Not needed to run the app in production.
#   pip install -r requirements.txt -r requirements-cv.txt -r requirements-dev.txt
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.27.2
ruff==0.8.6
